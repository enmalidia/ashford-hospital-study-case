import styled from "styled-components"
import {Card} from "../../styles/global"

export const EspecialtyCard = styled(Card).attrs({
    className: "flex"
})
`
    flex-direction: row;
    height: auto;
    padding: 24px;
    margin: 8px;    
`;